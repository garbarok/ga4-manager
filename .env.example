# ==========================================
# GA4 Manager Environment Configuration
# ==========================================
#
# IMPORTANT: This is an example file. Copy it to .env and fill in your actual values.
#
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder values with your actual credentials
# 3. NEVER commit the .env file to git (it's in .gitignore)
# 4. Keep your credentials file outside the repository directory
#
# ==========================================

# Google Cloud credentials file path
# This should point to your service account JSON key file
# Example: /Users/yourusername/.config/gcloud/ga4-manager-credentials.json
#
# SECURITY: Store credentials OUTSIDE this repository directory
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/credentials.json

# Google Cloud project ID
# Find this in the Google Cloud Console
# Example: my-analytics-project-123456
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# ==========================================
# Security Best Practices:
# ==========================================
#
# 1. Store credentials in a secure location (e.g., ~/.config/gcloud/)
# 2. Set restrictive file permissions: chmod 600 ~/.config/gcloud/credentials.json
# 3. Use a service account with minimal required permissions
# 4. Regularly rotate service account keys
# 5. Never share credentials or commit them to version control
# 6. Use separate service accounts for development and production
#
# Required OAuth Scopes:
# - https://www.googleapis.com/auth/analytics.edit
# - https://www.googleapis.com/auth/analytics.readonly
# ==========================================
